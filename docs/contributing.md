# Contributing

See [CONTRIBUTING.md](https://github.com/canact/canact/blob/main/CONTRIBUTING.md)
in the repository.

Local gate: `make check`. Commits need a DCO sign-off
(`git commit -s`). PR titles use conventional types
(`feat`, `fix`, `perf`, `docs`, `chore`, `ci`).
