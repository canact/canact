# Release notes

Optional curated body for a GitHub Release. Name the file after the
tag (`v0.1.2.md`). The file stays in git. The cargo-dist host job,
or `Apply release notes` on `workflow_dispatch`, copies it onto the
Release page. [CHANGELOG.md](../../CHANGELOG.md) stays the
conventional-commit list. There is no cleanup PR.
